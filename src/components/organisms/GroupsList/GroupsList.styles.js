import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    groupItem: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 10,
    },
    groupImage: {
        width: 50,
        height: 50,
        borderRadius: 25,
        marginRight: 10,
    },
    groupName: {
        fontSize: 18,
    },
});