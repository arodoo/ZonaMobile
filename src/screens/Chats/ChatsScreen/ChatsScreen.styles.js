import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    groupItem: {
        flexDirection: "row",
        alignItems: "center",
        padding: 16,
        borderBottomWidth: 1,
        borderBottomColor: "#E0E0E0",
    },
    groupName: {
        fontSize: 16,
        fontWeight: "bold",
    },
    
});